name: Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: Show runner info
        run: |
          echo "Runner is working on $(hostname)"
          uname -a
