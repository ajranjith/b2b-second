"use client";

import { Card, CardContent } from "@repo/ui";

export default function DealerBackordersPage() {
  return (
    <Card>
      <CardContent className="py-16 text-center text-slate-500">
        Backorders tracking will launch in Phase 2.
      </CardContent>
    </Card>
  );
}
